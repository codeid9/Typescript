# Basic Types in TypeScript

## Covered

- Primitives: string, number, boolean
- Arrays and objects
- Optional properties
- Function parameters and return types

## Example

```ts
type User = {
  id: number;
  username: string;
  isAdmin?: boolean;
};
```
